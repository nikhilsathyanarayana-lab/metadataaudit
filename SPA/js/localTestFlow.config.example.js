const localTestFlowConfig = {
  enabled: true,
  api: 'your-test-api-id.eu',
  subKey: 'your-test-sub-key',
  autoRunOnCredentialChange: true,
  debounceMs: 600,
};

export default localTestFlowConfig;
